# Optional local overrides. If this file is absent, defaults are public registries.
# Uncomment only when you need a private PyPI / Docker mirror on your network:
# DOCKER_PYTHON_IMAGE=python:3.12-slim
# PYPI_INDEX_URL=https://pypi.org/simple/
# PIP_TRUSTED_HOST=
# POC_BASE_IMAGE=python:3.12-slim

DOCKER_PYTHON_IMAGE=python:3.12-slim
PYPI_INDEX_URL=https://pypi.org/simple/
PIP_TRUSTED_HOST=
POC_BASE_IMAGE=python:3.12-slim
