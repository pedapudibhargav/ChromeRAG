# Highlights — paste into Editorial Manager (SoftwareX)

Elsevier SoftareX asks for **3–5 bullet points**, each **≤ 85 characters** (including spaces).
These appear with the online article for search discoverability. They are **not** a section you
must invent inside the Word template body — EM has a dedicated Highlights step/fields.

Copy the five lines below into EM exactly (one bullet per field, or as the form instructs):

```
ChromeRAG turns scraped HTML into RAG-ready Markdown with site-chrome removal.
STCE learns recurring nav/footer chrome across a corporate domain before extract.
On 238 scoreable pages, coverage mode reaches Fbal 0.800 vs Trafilatura 0.752.
Thin JS shells warn the caller: render with Playwright first, then extract.
MIT CLI + static Compare Lab; deterministic Recall / Noise / Fbal benchmarks.
```

Character counts (must be ≤ 85):

| # | Chars | Highlight |
|---|------:|-----------|
| 1 | 78 | ChromeRAG turns scraped HTML into RAG-ready Markdown with site-chrome removal. |
| 2 | 81 | STCE learns recurring nav/footer chrome across a corporate domain before extract. |
| 3 | 78 | On 238 scoreable pages, coverage mode reaches Fbal 0.800 vs Trafilatura 0.752. |
| 4 | 75 | Thin JS shells warn the caller: render with Playwright first, then extract. |
| 5 | 77 | MIT CLI + static Compare Lab; deterministic Recall / Noise / Fbal benchmarks. |
